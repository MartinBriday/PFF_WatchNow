import { OeuvreGroupOption } from './oeuvre-group-option.model';

describe('OeuvreGroupOption', () => {
  it('should create an instance', () => {
    expect(new OeuvreGroupOption()).toBeTruthy();
  });
});
