import { OeuvrePlotSummary } from './oeuvre-plot-summary.model';

describe('OeuvrePlotSummary', () => {
  it('should create an instance', () => {
    expect(new OeuvrePlotSummary()).toBeTruthy();
  });
});
