import { OeuvreWatchProvider } from './oeuvre-watch-provider.model';

describe('OeuvreWatchProvider', () => {
  it('should create an instance', () => {
    expect(new OeuvreWatchProvider()).toBeTruthy();
  });
});
