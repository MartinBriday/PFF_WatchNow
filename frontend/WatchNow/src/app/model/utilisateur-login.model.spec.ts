import { UtilisateurLogin } from './utilisateur-login.model';

describe('UtilisateurLogin', () => {
  it('should create an instance', () => {
    expect(new UtilisateurLogin()).toBeTruthy();
  });
});
